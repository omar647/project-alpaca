# Screenshots

- `dashboard_paper.png` — Paper-trading dashboard: RUNNING status, live signals
  (LONG/FLAT), account KPIs, orders, and the event log. *(committed)*
- `dashboard_backtest.png` — Backtest view: equity curve vs Buy & Hold, metrics,
  and per-symbol comparison. *(committed)*
- `alpaca_dashboard.png` — *add yours:* your Alpaca **paper** account showing an
  executed order (for the submission/video).

Recapture the running app any time with:

```bash
streamlit run ui/app.py
```

(The `charts/` folder also holds committed backtest equity + drawdown PNGs
generated by the backtest engine.)
