# Screenshots

Add your own dashboard captures here for the README / submission:

- `dashboard_paper.png` — the Paper-trading dashboard (status RUNNING, positions,
  signals, P&L, event log).
- `dashboard_backtest.png` — the Backtest view (equity vs Buy & Hold, metrics).
- `alpaca_dashboard.png` — your Alpaca **paper** account showing an executed order.

Capture the running app with:

```bash
streamlit run ui/app.py
```

(The `charts/` folder already contains committed backtest equity + drawdown PNGs
generated by the backtest engine.)
