[theme]
base = "dark"
primaryColor = "#00e5a0"
backgroundColor = "#0a0d13"
secondaryBackgroundColor = "#10141d"
textColor = "#e8ebf2"
font = "sans serif"

[server]
headless = true

# Full hamburger (⋮) menu is kept (theme switcher, Settings, Rerun, Print, About).
# The "Deploy" button is hidden via CSS in ui/app.py instead of reducing the menu.
